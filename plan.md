# NameTag 전환 계획

## 목표
- 기존 FastAPI/React 구조를 main.md 기준의 섹션형 REST API + React 인터뷰 플로우로 전환한다.
- 백엔드를 먼저 새 구조로 정리한 뒤, 프론트엔드를 그 API에 맞게 교체한다.

## 진행 단계
1. 백엔드 구조 설계 확인 및 기존 엔트리/라우터 교체 [완료]
2. Gemini 서비스, 이미지 서비스, 프롬프트, PDF 빌더 재구성 [완료]
3. 섹션 O/A/B/C/DE/PDF 라우터 구현 및 정적 자산 서빙 설정 [완료]
4. 프론트엔드 상태 저장소, API 클라이언트, 공통 컴포넌트 구현 [완료]
5. 섹션별 페이지와 PDF 미리보기 흐름 연결 [완료]
6. 도커 실사용 검증 [환경 블록]

## 현재 상태
- 백엔드: 완료
- 프론트엔드: 완료
- 도커 실사용 점검: Docker daemon 미기동으로 현재 환경에서 컨테이너 기동은 불가

## 메모
- main.md의 엔드포인트 이름과 응답 구조를 우선으로 맞춘다.
- 이미지 경로는 `/assets/...` 상대경로를 웹 서빙 기준으로 유지한다.

## 다음 진행할 작업

### 1. PDF 미리보기 사이드바 정리
- 1-1. 미리보기 전용 사이드바 컴포넌트를 분리한다.
- 1-2. 브랜드 정보와 섹션 A/B/C/DE 데이터를 묶어서 HTML 미리보기를 만든다.
- 1-3. Generate의 Preview 단계에서 새 사이드바만 노출되도록 연결한다.
- 1-4. 입력값 변경 시 미리보기가 즉시 갱신되는지 확인한다.

### 2. PDF 변수 수정 사이드바 정리
- 2-1. 브랜드 기본 변수 편집 폼을 만든다.
- 2-2. Section A/B/C/DE의 JSON 객체를 직접 수정할 수 있게 한다.
- 2-3. 변수 수정 결과가 미리보기와 PDF 생성에 동일하게 반영되도록 한다.

### 3. 이미지가 PDF에 포함되지 않는 문제 해결
- 이미지 전달 경로, 정적 서빙, PDF 빌더의 참조 방식을 함께 점검한다.
- 원인을 좁힌 뒤 백엔드 또는 프론트엔드 한쪽에서 최소 수정으로 고친다.
- 이유: PDF 빌더가 `/assets/...` 웹 경로를 그대로 `os.path.exists`에 넣어서, 워크스페이스의 실제 파일을 찾지 못했다.
- 해결 방법: PDF 빌더 내부에서 `/assets/...`를 워크스페이스 절대 파일 경로로 변환하고, WeasyPrint는 파일 URL 없이 로컬 파일을 직접 읽도록 정리한다.
- 결과: 로고와 캐릭터 이미지가 PDF 본문에 포함되도록 경로 처리 흐름을 바로잡는다.

### 4. 디버깅 메시지 추가
- 프론트엔드에서 섹션별 payload와 API 호출 상태를 로그로 남긴다.
- 백엔드에서 PDF 생성 과정과 이미지 처리 과정을 추적할 수 있게 한다.
- 이유: 지금은 화면상 결과만 보여서, 어떤 섹션 입력이 PDF/이미지 생성 실패로 이어졌는지 역추적하기 어렵다.
- 해결 방법: 프론트엔드는 섹션별 request/response/저장 시점 로그를 남기고, 백엔드는 이미지 생성과 PDF 조립 전후에 경로·키·길이를 남긴다.
- 결과: 실패 지점이 API 호출, 이미지 저장, PDF 렌더링 중 어디인지 로그만 보고 바로 좁힐 수 있다.

### 5. 도커 실사용 검증
- 로컬 기능이 안정화되면 Docker 환경에서 실제 기동을 다시 확인한다.
- 현재 환경에서는 Docker daemon이 미기동이므로, 실행 가능 환경에서 최종 검증한다.
- 이유: 백엔드 Docker 이미지가 WeasyPrint 런타임 라이브러리를 빠뜨려서 컨테이너가 시작 직후 종료됐다.
- 해결 방법: backend/Dockerfile에 WeasyPrint가 요구하는 native 라이브러리들을 추가하고, frontend 이미지는 별도 빌드 후 `docker compose up -d --no-build`로 기동했다.
- 결과: `docker compose ps`에서 backend/frontend가 모두 Up 상태로 올라왔고, `http://localhost:8000/health`와 `http://localhost:3000` 응답을 확인했다.