# NameTag 전환 계획

## 목표
- 기존 FastAPI/React 구조를 main.md 기준의 섹션형 REST API + React 인터뷰 플로우로 전환한다.
- 백엔드를 먼저 새 구조로 정리한 뒤, 프론트엔드를 그 API에 맞게 교체한다.

## 진행 단계
1. 백엔드 구조 설계 확인 및 기존 엔트리/라우터 교체 [완료]
2. Gemini 서비스, 이미지 서비스, 프롬프트, PDF 빌더 재구성 [완료]
3. 섹션 O/A/B/C/DE/PDF 라우터 구현 및 정적 자산 서빙 설정 [완료]
4. 프론트엔드 상태 저장소, API 클라이언트, 공통 컴포넌트 구현 [완료]
5. 섹션별 페이지와 PDF 미리보기 흐름 연결 [완료]
6. 도커 실사용 검증 [환경 블록]

## 현재 상태
- 백엔드: 완료
- 프론트엔드: 완료
- 도커 실사용 점검: Docker daemon 미기동으로 현재 환경에서 컨테이너 기동은 불가

## 메모
- main.md의 엔드포인트 이름과 응답 구조를 우선으로 맞춘다.
- 이미지 경로는 `/assets/...` 상대경로를 웹 서빙 기준으로 유지한다.

## 다음 진행할 작업

### 1. 과거 파일 줄 태니까 과거 파일처럼 프론트엔드 수정할 것
- 이전 구조/패턴에 맞게 프론트엔드 리팩토링
- 레이아웃 및 컴포넌트 구조 정리

### 2. 사이드 바 추가 - PDF 미리보기
- 새로운 사이드 바 컴포넌트 생성
- PDF 생성 전 HTML 미리보기 기능 추가
- 실시간 HTML 렌더링으로 변경사항 즉시 확인 가능하도록 구현

### 3. 사이드 바 추가 - PDF 변수 수정
- 또 다른 사이드 바 컴포넌트 생성
- PDF에 들어갈 변수들을 수정할 수 있는 UI 제공
- 변수 값 변경 시 미리보기 업데이트

### 4. 이미지 PDF 포함 문제 해결
- 현재 이미지가 PDF에 들어가지 않는 원인 파악
- 백엔드 및 프론트엔드 이미지 전달 방식 점검
- 이미지 포함 기능 수정 및 정상 작동 확인

### 5. 디버깅 메세지 추가
- 프론트엔드: 각 섹션별 데이터 전달 및 API 호출 상황 로깅
- 백엔드: PDF 생성 과정, 이미지 처리 과정 등 상세 로깅
- 콘솔/로그 뷰어에서 디버깅 정보 확인 가능하도록 구현