# 작업 로그 — 2026-05-31

요약
- 오늘 목표: 현재 프론트엔드를 과거 `past/frontend` 컨셉과 일치시키고, 백엔드와 연동하여 실제 생성(인터뷰, 후보, 아이덴티티, 로고·캐릭터)까지 실행해서 아티팩트를 제공.

환경
- OS: Linux (dev container)
- 프론트엔드: Vite + React + TypeScript + Tailwind
- 백엔드: FastAPI (uvicorn), google-genai(Gemini), image generation
- 주요 포트: 프론트엔드 개발서버 `http://localhost:5174/`, 백엔드 API `http://localhost:8000`
- 중요한 파일: `backend/.env` (GEMINI_API_KEY, GEMINI_IMAGE_API_KEY 있음)

주요 작업(순서)
1. 프론트엔드 라우팅/레이아웃 복원
   - `frontend/src/App.tsx`를 Router 기반으로 변경하여 `Home` 및 `Generate` 페이지로 구성함.
   - 새로 추가/수정: `frontend/src/pages/Home.tsx`, `frontend/src/pages/Generate.tsx`, `frontend/src/components/Layout/Header.tsx`, `frontend/src/components/Layout/Footer.tsx`, `frontend/src/components/Wizard/ProgressBar.tsx` 등.
2. 개발서버 빌드 및 실행
   - `npm install` 및 `npm run build` 성공.
   - Vite 개발서버 실행: Local `http://localhost:5174/`에서 동작.
3. 백엔드 실행 및 디버깅
   - `pip install -r backend/requirements.txt` 실행.
   - `python -m uvicorn main:app --host 0.0.0.0 --port 8000`로 백엔드 실행.
   - 초기에는 포트 충돌 문제 해결(기존 프로세스 종료).
   - `backend/.env`에 GEMINI 관련 키가 존재하여 실제 Gemini 호출이 가능함.
4. 섹션 O 인터뷰 / 후보 생성 테스트
   - `/api/section-o/interview`에 샘플 `brand_data`로 POST 요청하여 인터뷰 질문 생성 확인.
   - `/api/section-o/candidates`로 MVB 후보 생성 확인.
5. DE(아이덴티티) 생성 및 이미지 생성
   - `/api/section-de/generate` 호출로 Gemini 텍스트 응답을 받아 아이덴티티(logo_identity, character_guide) 생성.
   - `services/image_service.generate_logo_image` 및 `generate_character_image`를 통해 이미지 생성, `assets/logos` 및 `assets/characters`에 파일 저장됨.
6. 생성 아티팩트 수집 및 압축
   - 생성된 파일: `assets/logos/logo_누에_20260531_010812.png`, `assets/characters/char_누에_20260531_010904.png` 등.
   - 압축파일 생성: `artifacts_nametag_20260531.zip` (logos + characters 포함).
7. 프론트엔드에 아티팩트 연결
   - `assets` 내 생성된 로고/캐릭터를 `frontend/logo`로 복사하여 개발서버 정적 경로에서 제공하도록 함.
   - `frontend/src/components/Layout/Header.tsx`를 수정해 `logo_black.png` / `logo_white.png`를 사용하도록 변경.
   - `frontend/src/pages/Home.tsx`의 상단 배너 문구 제거하고 기존 로고(black/white)와 생성된 로고/캐릭터 썸네일을 표시하도록 수정.
   - `frontend/src/components/RightSidebar.tsx`의 미리보기 탑에 생성된 로고/캐릭터 썸네일 추가.

변경된 주요 파일
- `frontend/src/App.tsx` (라우팅 변경)
- `frontend/src/pages/Home.tsx` (로고/배너 변경)
- `frontend/src/pages/Generate.tsx` (생성 워크플로우 쉘)
- `frontend/src/components/Layout/Header.tsx` (로고 표시)
- `frontend/src/components/RightSidebar.tsx` (썸네일 표시)
- `backend/services/gemini_service.py` (개발 중이던 mock 제거 — 현재 실제 GEMINI 키 필요)
- `backend/services/image_service.py` (이미지 저장 로직)

생성된 아티팩트 경로
- 로고: `assets/logos/logo_누에_20260531_010812.png`
- 캐릭터: `assets/characters/char_누에_20260531_010904.png`
- 압축: `artifacts_nametag_20260531.zip`
- 프론트엔드 정적 사본: `frontend/logo/logo_누에_20260531_010812.png`, `frontend/logo/logo_onoff_20260531.png`, `frontend/logo/char_onoff_20260531.png`

현재 상태 및 주의사항
- 백엔드가 실제 Gemini API를 호출하므로, 사용량/비용에 유의해야 함.
- 로컬에서 동일 재현하려면 `backend/.env`에 유효한 Gemini 키가 필요함.
- 일부 변경(프론트엔드 파일 추가)은 아직 커밋되지 않았습니다 — 필요하면 커밋/PR 생성 가능.

다음 권장 작업
- RightSidebar가 API로 받은 `data_de.logo_path`/`char_path`를 우선적으로 렌더하도록 연결 (현재는 정적 파일을 표시함).
- UI 스타일 조정: 로고/썸네일 사이즈, 라이트/다크 모드 일관성 검토.
- 필요 시 추가 입력(브랜드 데이터)으로 여러 후보/로고를 대량 생성.

추가 요청이 있으면 알려주세요.
